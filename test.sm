main :: proc()
{
    var := 4;
    
    
    if var < 5 do
        if var < 3 do
        var = 3;
    
    // var = square(sub(var, 3));
    
    return var;
}

square :: proc(a: i64) -> i64
{
    return a * a;
}

sub :: proc(a: i64, b: i64) -> i64
{
    return a - b;
}
