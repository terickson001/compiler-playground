main :: proc()
{
    var := 5;
    
    
    if var < 5
    {
        if var < 3 do
            var = 3;
    }
    
    
    var = square(sub(var, 3));
    
    return var;
}

square :: proc(a: i64) -> i64
{
    return a * a;
}

sub :: proc(a: i64, b: i64) -> i64
{
    return a - b;
}
